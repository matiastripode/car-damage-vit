# Dependencias para CI — subconjunto de requirements.txt
# Excluye fiftyone (MongoDB, muy pesado) y datasets (descarga HuggingFace)
# PyTorch se instala aparte como CPU-only en el workflow

transformers>=4.35.0
Pillow>=10.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
pycocotools>=2.0.7
pyyaml>=6.0
fastapi>=0.104.0
httpx>=0.25.0
pytest>=7.4.0
